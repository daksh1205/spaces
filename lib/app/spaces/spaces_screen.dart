import 'package:flutter/material.dart';

class ThiskritSpacesScreen extends StatelessWidget {
  const ThiskritSpacesScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: SafeArea(child: Column(children: [
          
        ],)),
    );
  }
}
